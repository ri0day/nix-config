keys:
  - &masonmactest age12cyv0vfpfp5jwdkzgena7df4pg4hj0x83yqdlckl5cxlxaxqgveq9s7d7u

creation_rules:
  # Shared secrets
  - path_regex: secrets/shared\.yaml$
    key_groups:
      - age:
          - *masonmactest

  # Host-specific secrets (m1max)
  - path_regex: hosts/m1max/secrets\.yaml$
    key_groups:
      - age:
          - *masonmactest

  # Host-specific secrets (work-mac)
  - path_regex: hosts/work-mac/secrets\.yaml$
    key_groups:
      - age:
          - *masonmactest

  # Host-specific secrets (nix-server)
  - path_regex: hosts/nix-server/secrets\.yaml$
    key_groups:
      - age:
          - *masonmactest

  # Host-specific secrets (ubuntu-server)
  - path_regex: hosts/ubuntu-server/secrets\.yaml$
    key_groups:
      - age:
          - *masonmactest

  # Legacy secrets (for backward compatibility during migration)
  - path_regex: secrets/[^/]+\.(yaml|json|env|ini)$
    key_groups:
      - age:
          - *masonmactest
