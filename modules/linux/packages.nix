{ pkgs, ... }:

{
  # Non-NixOS Linux (Ubuntu, etc.) System Packages
  # This is a stub file for non-NixOS Linux hosts
  # Note: On non-NixOS Linux, system packages are managed by the host OS
  # This file can be used for reference or for packages installed via nix-env

  # These packages would need to be installed via nix-env or the host package manager
  # Example packages that might be useful on a server:
  # environment.systemPackages = with pkgs; [
  #   htop
  #   iotop
  #   ncdu
  #   tmux
  # ];
}
